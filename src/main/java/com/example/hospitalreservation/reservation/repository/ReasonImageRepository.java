package com.example.hospitalreservation.reservation.repository;

import com.example.hospitalreservation.reservation.entity.ReasonImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReasonImageRepository extends JpaRepository<ReasonImage, Long> {
}